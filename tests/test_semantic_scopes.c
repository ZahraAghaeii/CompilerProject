int global_var = 100;

// خطا: پارامتر تکراری
int complex_func(int param, int param) {
    // هشدار: سایه‌اندازی روی متغیر سراسری
    int global_var = 200;

    // خطا: تعریف مجدد param (چون به عنوان پارامتر در همین اسکوپ وجود دارد)
    int param = 5;

    int uninit_var;

    {
        // هشدار: سایه‌اندازی دوباره! این بار روی global_var تابع
        int global_var = 300;

        // این متغیر فقط در این بلوک زنده است
        int block_var = 50;

        // هشدار: استفاده از متغیر مقداردهی نشده مربوط به اسکوپ بالاتر
        int z = uninit_var;
    }

    // نقطه کور: تلاش برای دسترسی به متغیری که در بلوک داخلی بوده و الان مرده است!
    block_var = 10;

    return 0;
}